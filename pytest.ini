[pytest]
testpaths = src/main/api/tests tests/unit
pythonpath = .
python_files = test_*.py
addopts = -ra --alluredir=allure-results --clean-alluredir
markers =
    api: API tests requiring the local Bank API
